"""Audio processing tab: the pipeline stages run over the audio inputs."""

from __future__ import annotations

from body_eye_sync.gui.tabs.base import PlaceholderTab


class AudioProcessingTab(PlaceholderTab):
    """Run the audio pipeline over the experiment's audio inputs."""

    title = "Audio processing"
